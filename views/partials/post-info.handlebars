<article class="post">
    <div class="title">
        <a href="/post/{{id}}">
            <h2>{{title}}</h2>
        </a>
    </div>
    <p>{{content}}</p>
    <div class="meta">
        |
        <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    </div>
</article>